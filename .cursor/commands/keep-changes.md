# keep-changes

Read every file again before you change anything. Ignore old copies
from this chat.

Treat the current on-disk contents as the source of truth. Keep the
user's manual edits — never restore or overwrite them with an older
version.
